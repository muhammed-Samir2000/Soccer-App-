# Optional public Supabase values used only while Docker builds Flutter Web.
# Never put the Supabase service-role key here.
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Host port for the local or server deployment.
APP_PORT=8080
