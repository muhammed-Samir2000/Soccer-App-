server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Flutter uses a single application shell; preserve deep links on refresh.
  location / {
    try_files $uri $uri/ /index.html;
  }

  location = /index.html {
    add_header Cache-Control "no-cache, no-store, must-revalidate";
  }

  # These files decide which Flutter release the browser boots. Caching them
  # can keep an old service worker and an old application alive after deploys.
  location = /flutter_service_worker.js {
    add_header Cache-Control "no-cache, no-store, must-revalidate";
  }

  location = /flutter_bootstrap.js {
    add_header Cache-Control "no-cache, no-store, must-revalidate";
  }

  location = /manifest.json {
    add_header Cache-Control "no-cache, no-store, must-revalidate";
  }

  location ~* \.(?:js|css|png|jpg|jpeg|gif|svg|ico|woff2?)$ {
    expires 7d;
    add_header Cache-Control "public";
  }
}
